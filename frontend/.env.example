BACKEND_URL=http://localhost:5000
# Optional: set this only if the browser should call the API directly instead of using Next rewrites.
# NEXT_PUBLIC_API_URL=http://localhost:5000

# Public booking link for the /schedule page. Use Calendly, TidyCal, Google Calendar appointment schedule, or Zoom Scheduler.
# NEXT_PUBLIC_SCHEDULING_URL=https://calendly.com/syntrixlabs/free-consultation
